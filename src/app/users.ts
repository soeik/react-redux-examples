import { User } from "@/types";
import { faker } from "@faker-js/faker";

// User
// {
//   id: faker.string.uuid(),
//   name: faker.person.lastName(),
//   clickCount: 0,
// }

// #1 Реализовать функцию, которая бы возвращала заполненный массив пользователей заданной длины
export const getUsers = (usersCount: number): Array<User> => {};
